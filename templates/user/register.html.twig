{% extends 'base.html.twig' %}

{% block body %}
    <h1>User Registration</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn">Register</button>
    {{ form_end(form) }}
{% endblock %}
